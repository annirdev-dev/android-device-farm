node_modules/
dist/
build/
.next/
out/
.turbo/
*.tsbuildinfo
.env
.env.*
!.env.example
!.env.*.example
.DS_Store
coverage/
*.log
npm-debug.log*
pnpm-debug.log*

# Prisma
packages/database/generated/

# Emulator runtime state (never commit device data)
var/emulators/
*.avd/
*.qcow2
*.img

# Object storage local dev volume
infrastructure/docker/data/

# Railway IaC authoring file - contains generated secrets inline (JWT
# secret, MinIO credentials); kept local-only, never pushed. Only
# .railway/README.md (Railway's own generated doc, no secrets) is tracked.
.railway/railway.ts
.railway/railway.py
.railway/railway.go
